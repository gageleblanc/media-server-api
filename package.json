{
  "name": "media-server-api",
  "version": "1.0.0",
  "description": "API to spawn upload-slave processes. There will be a plugin for the cli tool to upload files from the command line. ",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
